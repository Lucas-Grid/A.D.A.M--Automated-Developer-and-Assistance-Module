# Runtime dependencies for Jarvis.
# Install with:  pip install -r requirements.txt
# (The hermes-agent venv may need:  python -m ensurepip --upgrade  first.)
fastapi>=0.110
requests>=2.28
pyautogui>=0.9.54
pillow>=10.0
pytesseract>=0.3.10

# Optional (uncomment if needed):
# playwright>=1.40        # browser automation tool
# docker>=7.0             # Docker sandbox isolation

# Holographic GUI (serves jarvis/gui/holo.html; websockets is the only hard dep)
websockets>=12.0

# Voice INPUT (speech-to-command). Pick ONE backend:
#   Offline (recommended, no API key): vosk + a model from https://alphacephei.com/vosk/models
#     pip install vosk  + set VOSK_MODEL_PATH to the extracted model folder
#   Online (Google STT):  pip install SpeechRecognition pyaudio
# Both are OPTIONAL -- Jarvis runs without them; voice input just reports
# "not installed" instead of failing.
# vosk>=0.3.45
# SpeechRecognition>=3.10
# pyaudio>=0.2.14
